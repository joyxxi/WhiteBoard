package View;

import javax.swing.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

/**
 * The main Frame class that holds the WhiteboardView and ToolboxView.
 */
public class MainFrame extends JFrame {
  public static final int WIDTH = 600;
  public static final int HEIGHT = 500;

  /**
   * Constructor to build the initial window.
   */
  public MainFrame(String title, WhiteBoardView whiteBoardView, ToolboxView toolboxView) {
    // TODO: construct the main view of the white board
  }
}
